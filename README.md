# act-interfaces

Bootstrap repository for versioned, language-neutral contracts shared by Anticaptrad services.

The first reviewed contract set will be added through a feature-branch pull request. This repository must never contain runtime credentials, OAuth tokens, API keys, or deployment secrets.
